# Build-Fleet DAG — 2026-05-13-fleet-all-approved

## Inverted file → proposals index

All `files_touched[]` sets from the per-proposal design.md files are disjoint. No file is written by more than one proposal in this fleet.

## Edges

None. No proposal reads a file another proposal writes. No write conflicts.

## Conflicts

None.

## Wave plan

**Wave 0 — three agents in parallel:**
- PROP-0001 (Census)
- PROP-0002 (Member-Quote export gap)
- PROP-0003 (Post-issuance AddMember)

PROP-0009 is **not in any wave** — it's already shipped per its own frontmatter (`status: done`); the index is the only thing that needs updating, which happens at SHIP.

## Commit-order for merge-back

Alphabetical PROP id: 0001 → 0002 → 0003.

## Generated-file exclusion

`schemas/forms/index.ts` is excluded from agent write sets (generated by `scripts/generate_form_index.mjs`). Orchestrator runs the generator once after the wave's cherry-picks and commits as `chore(forms): regenerate registry after fleet 2026-05-13-fleet-all-approved merge` if a diff appears.
