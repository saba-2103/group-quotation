{
  "id": "quote-census-tab",
  "type": "tab-panel",
  "props": { "value": "census", "label": "Census", "icon": "Users" },
  "children": [
    {
      "id": "quote-census-grid",
      "type": "key-value-grid",
      "dataSource": {
        "api": { "endpoint": "/api/quotation/quotes/{{id}}", "method": "GET" }
      },
      "props": {
        "title": "Aggregate census (read-only for V1 demo — D2)",
        "description": "File-format upload + per-plan headcount editing deferred to D2.",
        "columns": 2,
        "fields": [
          { "id": "headcount", "label": "Total headcount", "accessorKey": "headcount" },
          { "id": "censusFileFormat", "label": "Census file format", "accessorKey": "censusFileFormatJson", "type": "presence" }
        ]
      }
    }
  ]
}
